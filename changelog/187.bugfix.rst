 Fixed a ``KeyError`` raised when accessing the ``level`` and ``version`` properties of an ``RFSSpectrogram`` constructed from a real Parker Solar Probe RFS CDF file via `~radiospectra.spectrogram.spectrogram_factory.SpectrogramFactory`.
