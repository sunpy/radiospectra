Fixed a ``KeyError`` raised when accessing ``RFSSpectrogram.level`` and ``RFSSpectrogram.version`` on Parker Solar Probe RFS CDF files loaded through ``Spectrogram`` by standardizing CDF metadata storage under ``cdf_meta``.
