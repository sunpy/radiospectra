Fix metadata key mismatch between ``SpectrogramFactory`` and ``RFSSpectrogram`` the factory stored CDF global attributes under ``"cdf_globals"`` but ``RFSSpectrogram`` expected ``"cdf_meta"`` updated to use ``"cdf_globals"`` in both.
